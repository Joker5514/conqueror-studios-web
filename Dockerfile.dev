FROM oven/bun:1.3-alpine
WORKDIR /app

# Install deps as root, then drop privileges for runtime.
COPY package.json bun.lock* ./
RUN bun install --frozen-lockfile

COPY . .
RUN chown -R bun:bun /app
USER bun

EXPOSE 3000
CMD ["bun", "run", "dev"]
